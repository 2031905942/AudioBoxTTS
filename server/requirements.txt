# IndexTTS2 cloud service requirements (ASCII-only for Windows pip)

# Web framework
fastapi>=0.109.0
uvicorn[standard]>=0.27.0
pydantic>=2.5.0
pydantic-settings>=2.1.0

# Async/multipart
python-multipart>=0.0.6

# Optional audio processing (for format conversion)
# pydub>=0.25.1
